spring:
  application:
    name: dnk
  datasource:
    url: jdbc:postgresql://aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres
    username: postgres.xrkeaanesupertdozcyz
    password: TDungx28112003
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  validation:
    enabled: true

api:
  prefix: /api/v1

keys:
  private: PRIVATE_KEY
  public: PUBLIC_KEY
